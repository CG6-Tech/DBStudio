# Minimal Scrollbars Implementation Plan

1. Add global standards-based and WebKit scrollbar styling with 6px dimensions, transparent tracks, rounded muted thumbs, and hover/active feedback.
2. Remove the virtual table list's reserved scrollbar gutter.
3. Run the production build and CSS diff checks.
