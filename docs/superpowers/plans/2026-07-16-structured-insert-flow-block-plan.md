# Structured INSERT Flow Block Implementation Plan

1. Add a source-preserving INSERT projector with quote-, dollar-string-, cast-, and nesting-aware list splitting.
2. Add value classification and audit INSERT parser fixtures.
3. Attach structured INSERT details to SQL routine-flow nodes without replacing source SQL.
4. Add a reusable compact/expanded operation-mapping body with semantic chips.
5. Refresh measured circular-port geometry when block height changes.
6. Run focused tests, the full suite, web build, and macOS build.
