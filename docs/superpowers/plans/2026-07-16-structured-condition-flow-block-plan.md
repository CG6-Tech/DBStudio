# Structured Condition Flow Block Implementation Plan

1. Add a source-preserving condition projector that splits top-level boolean clauses safely and normalizes common PL/pgSQL comparison operators for display.
2. Extend routine-flow condition details with guard, decision, and shared-subject switch metadata while retaining safe generic fallbacks.
3. Derive conservative semantic output labels from trigger operations, immediate returns, and immediate exceptions.
4. Add a reusable structured condition body with clause rows, logic rails, switch subject display, and an expandable original expression.
5. Integrate condition-specific geometry and styling with the existing measured circular-port system and natural connection router.
6. Add parser, graph, and component tests, then run the complete suite, web build, and native macOS build.
